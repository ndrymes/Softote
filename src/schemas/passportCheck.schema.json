{
  "type": "object",
  "additionalProperties": false,
  "required": ["passportId"],
  "properties": {
    "passportId": {
      "type": "string",
      "description": "its a string that is assigned to a user for a means of verification",
      "examples": [ "BAS3333445" ]
    }
  }
}