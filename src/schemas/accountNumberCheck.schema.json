{
  "type": "object",
  "additionalProperties": false,
  "required": ["accountNumber"],
  "properties": {
    "accountNumber": {
      "type": "string",
      "pattern": "^\\d+$",
      "errorMessage": {
        "pattern": "accountNumber must contain only digits"
      },
      "description": "its a number that is assigned to a user for a means of verification",
      "examples": [ "11113333445" ],
      "minLength": 10
    }
  }
}