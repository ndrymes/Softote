{
  "type": "object",
  "additionalProperties": false,
  "required": ["vnin"],
  "properties": {
    "vnin": {
      "type": "string",
      "pattern": "^\\d+$",
      "errorMessage": {
        "pattern": "vnin must contain only digits"
      },
      "description": "its a number that is assigned to a user for a means of verification",
      "examples": [ "11113333445" ]
    }
  }
}
